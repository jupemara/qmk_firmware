# The default keymap for Kudox Keyboard Rev3.0

## build

before execute following command please confirm to build which hand mode in `keyboards/kudox/config.c`.

```sh
$ qmk flash -kb kudox/rev3 -km jupemara
```